{{ form_start(form) }}


    <div class="m-5">
        {#<div class="h2 mb-2">
        {{"Message"}}
        </div>#}
        <div>
            {{ form_widget(form.message) }}
        </div>
    </div>
    

 <button class="btn btn-info m-5">{{ button_label|default('Envoyer')  }}</button>

{{ form_end(form) }}
